@using test.Models.ViewModels
@model  IEnumerable<PupilViewModel>
<style type="text/css">
    caption {
        display: table-caption;
        text-align: center;
    }
</style>
<table class="table table-striped table-bordered">
    <caption>Pupils</caption>
    <thead class="thead-dark">
        <tr>
            <th>Name</th>
            <th>Surname</th>
            <th>Gender</th>
            <th>Actions</th>
        </tr>
    </thead>
    <tbody>
        @foreach (PupilViewModel item in Model)
        {
            <tr>
                <td>@item.Name</td>
                <td>@item.Surname</td>
                <td>@item.Gender</td>
                <td>@Html.ActionLink("Edit", "EditPupil", "Pupil", new { id = item.Id }, null) | @Html.ActionLink("Set", "SetMonitor", "Class", new { id = item.IdClass, vm = item.Id }, null)</td>
            </tr>
        }
    </tbody>
</table>